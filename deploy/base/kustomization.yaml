apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: finance

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: ghcr.io/petar-dev-port/finance-tracker
    newTag: latest