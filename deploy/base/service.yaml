apiVersion: v1
kind: Service
metadata:
  name: finance-tracker
  namespace: finance
  labels:
    app: finance-tracker
spec:
  type: ClusterIP
  selector:
    app: finance-tracker
  ports:
  - port: 80
    targetPort: 8080